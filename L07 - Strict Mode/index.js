'use strict';  // we cannot (use)

let user='Aravind'
use='Arun'
console.log(use);// error identifying

const interface='car' // we cannot use variable name as interface
const private='bike'// we cannot use variable name as private